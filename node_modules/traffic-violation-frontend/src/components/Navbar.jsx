import { useState, useEffect, useRef } from 'react'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import Logo from './Logo'
import GooeyNav from './GooeyNav'
import NotificationWidget from './NotificationWidget'

function Navbar({ user, onLogout }) {
  const location = useLocation()
  const navigate = useNavigate()
  const [isProfileOpen, setIsProfileOpen] = useState(false)
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false)
  const dropdownRef = useRef(null)
  const mobileMenuRef = useRef(null)

  const homePath = '/hero'

  useEffect(() => {
    function handleClickOutside(event) {
      if (dropdownRef.current && !dropdownRef.current.contains(event.target)) {
        setIsProfileOpen(false)
      }
      if (mobileMenuRef.current && !mobileMenuRef.current.contains(event.target)) {
        setIsMobileMenuOpen(false)
      }
    }
    document.addEventListener('mousedown', handleClickOutside)
    return () => document.removeEventListener('mousedown', handleClickOutside)
  }, [])

  useEffect(() => {
    setIsMobileMenuOpen(false)
  }, [location.pathname])

  const handleLogout = () => {
    setIsProfileOpen(false)
    onLogout()
    navigate('/')
  }

  const getUserInitials = () => {
    if (!user?.full_name && !user?.name) return 'U'
    const name = user.full_name || user.name || 'User'
    return name.split(' ').map(n => n[0]).join('').toUpperCase().slice(0, 2)
  }

  const getRoleBadge = () => {
    if (!user?.role) return null
    return user.role === 'citizen' ? { label: 'Citizen' } : { label: 'Police' }
  }

  const roleBadge = getRoleBadge()

  const navItems = user?.role === 'police'
    ? [
        { name: 'Dashboard', path: '/police' },
        { name: 'Review Reports', path: '/police/review-reports' },
        { name: 'Review Appeals', path: '/police/review-appeals' },
        { name: 'Vehicle Search', path: '/vehicle-search' },
        { name: 'Analytics', path: '/analytics' },
        { name: 'Overdue Log', path: '/police/overdue-log' },
        { name: 'Rules & Laws', path: '/rules' },
        { name: 'Future Scopes', path: '/future-scopes' },
      ]
    : [
        { name: 'Dashboard', path: '/dashboard' },
        { name: 'Submit Report', path: '/submit-report' },
        { name: 'My Reports', path: '/my-reports' },
        { name: 'My Challans', path: '/my-challans' },
        { name: 'Analytics', path: '/analytics' },
        { name: 'Rules & Laws', path: '/rules' },
        { name: 'Future Scopes', path: '/future-scopes' },
      ]

  const gooeyItems = navItems.map(item => ({ label: item.name, href: item.path }))
  const activeGooeyIndex = Math.max(0, navItems.findIndex(item => item.path === location.pathname))

  return (
    <nav className="fixed top-5 left-8 right-8 z-50">
      <div className="max-w-[1600px] mx-auto">
        <div
          className="rounded-full px-10 py-3"
          style={{
            background: 'white',
            border: '1px solid rgba(0,0,0,0.15)',
            boxShadow: '0 10px 30px rgba(0,0,0,0.3), 0 0 0 1px rgba(0,0,0,0.05)',
          }}
        >
          <div className="flex justify-between items-center">
            {}
            <Link
              to={homePath}
              className="flex flex-col items-center flex-shrink-0 group"
              style={{ textDecoration: 'none' }}
            >
              <div className="flex items-center gap-3">
                <Logo className="h-8 w-auto" />
                <h1
                  className="text-xl font-bold whitespace-nowrap"
                  style={{ transition: 'color 0.2s', color: 'var(--text-primary)' }}
                >
                  Marga Rakshak
                </h1>
              </div>
              <p className="text-[9px] uppercase tracking-[0.22em] text-gray-400 font-bold text-center mt-0.5">
                Government of Tamil Nadu
              </p>
            </Link>

            {}
            <div className="hidden lg:flex items-center">
              <GooeyNav
                items={gooeyItems}
                initialActiveIndex={activeGooeyIndex}
                particleCount={12}
                particleDistances={[70, 8]}
                particleR={80}
                animationTime={500}
                timeVariance={250}
                colors={[1, 2, 3, 1, 2, 3, 1, 4]}
              />
            </div>

            {}
            <div className="flex items-center gap-3 flex-shrink-0">
              <NotificationWidget user={user} />
              <div className="relative" ref={dropdownRef}>
                <button
                  onClick={() => setIsProfileOpen(!isProfileOpen)}
                  className="flex items-center gap-3 p-2 rounded-full transition-colors"
                  style={{ transition: 'all 0.2s', background: isProfileOpen ? 'var(--bg-input)' : 'transparent' }}
                  onMouseEnter={e => e.currentTarget.style.background = 'var(--bg-input)'}
                  onMouseLeave={e => !isProfileOpen && (e.currentTarget.style.background = 'transparent')}
                >
                  <div className="w-9 h-9 bg-[#0a0a0a] rounded-full flex items-center justify-center text-white text-sm font-bold border-2 border-white/20">
                    {getUserInitials()}
                  </div>
                  <div className="hidden md:block text-left">
                    <p style={{ color: 'var(--text-primary)' }} className="text-sm font-semibold">{user?.full_name || user?.name || 'User'}</p>
                    {roleBadge && <p style={{ color: 'var(--text-secondary)' }} className="text-xs">{roleBadge.label}</p>}
                  </div>
                  <svg
                    style={{ color: 'var(--text-secondary)' }}
                    className={`w-4 h-4 transition-transform duration-200 ${isProfileOpen ? 'rotate-180' : ''}`}
                    fill="currentColor" viewBox="0 0 20 20"
                  >
                    <path fillRule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clipRule="evenodd" />
                  </svg>
                </button>

                {}
                {isProfileOpen && (
                  <div
                    className="absolute right-0 mt-2 w-64 rounded-[24px] py-2 animate-fade-in z-50 shadow-2xl"
                    style={{
                      background: 'var(--bg-card)',
                      border: '1.5px solid var(--border)',
                      boxShadow: 'var(--shadow)',
                    }}
                  >
                    <div className="px-4 py-3 border-b" style={{ borderColor: 'var(--border)' }}>
                      <p style={{ color: 'var(--text-primary)' }} className="text-sm font-semibold">{user?.full_name || user?.name || 'User'}</p>
                      <p style={{ color: 'var(--text-secondary)' }} className="text-xs mt-0.5">{user?.email || 'user@example.com'}</p>
                      {roleBadge && (
                        <span style={{ background: 'var(--bg-input)', color: 'var(--text-primary)' }} className="inline-block mt-2 px-3 py-1 rounded-full text-xs font-medium">
                          {roleBadge.label}
                        </span>
                      )}
                    </div>

                    <div className="py-2">
                      <Link
                        to="/profile"
                        onClick={() => setIsProfileOpen(false)}
                        className="flex items-center gap-3 px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50 transition-colors rounded-lg mx-2"
                      >
                        <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                        </svg>
                        Profile
                      </Link>
                      {user?.role === 'citizen' && (
                        <Link to="/rewards" onClick={() => setIsProfileOpen(false)}
                          className="flex items-center gap-3 px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50 transition-colors rounded-lg mx-2">
                          <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                          Rewards & Redeem
                        </Link>
                      )}
                      <Link
                        to="/settings"
                        onClick={() => setIsProfileOpen(false)}
                        className="flex items-center gap-3 px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50 transition-colors rounded-lg mx-2"
                      >
                        <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                        </svg>
                        Settings
                      </Link>
                    </div>

                    <div className="border-t border-gray-100 my-1" />

                    <button
                      onClick={handleLogout}
                      className="w-full flex items-center gap-3 px-4 py-2.5 text-sm text-red-600 hover:bg-red-50 transition-colors rounded-lg mx-2 text-left"
                      style={{ width: 'calc(100% - 16px)' }}
                    >
                      <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                      </svg>
                      Logout
                    </button>
                  </div>
                )}
              </div>

              {}
              <button
                onClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)}
                className="lg:hidden p-2 rounded-full hover:bg-gray-100 transition-colors"
              >
                {isMobileMenuOpen ? (
                  <svg className="w-6 h-6 text-gray-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
                  </svg>
                ) : (
                  <svg className="w-6 h-6 text-gray-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
                  </svg>
                )}
              </button>
            </div>
          </div>
        </div>
      </div>

      {}
      {isMobileMenuOpen && (
        <div className="lg:hidden mx-4 sm:mx-6 mt-2" ref={mobileMenuRef}>
          <div
            className="bg-white rounded-[24px] p-6 animate-slide-in"
            style={{ boxShadow: '0 0 0 1.5px rgba(0,0,0,0.08), 0 8px 32px rgba(0,0,0,0.14)' }}
          >
            <div className="space-y-1">
              {navItems.map((item) => {
                const isActive = location.pathname === item.path
                return (
                  <Link
                    key={item.path}
                    to={item.path}
                    className={`
                      block px-4 py-3 text-base font-medium rounded-xl transition-all
                      ${isActive
                        ? 'bg-gray-100 text-gray-900'
                        : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'
                      }
                    `}
                  >
                    {item.name}
                  </Link>
                )
              })}
              <button
                onClick={handleLogout}
                className="w-full text-left px-4 py-3 text-base font-medium text-red-600 hover:bg-red-50 rounded-xl transition-all border-t border-gray-100 mt-2 pt-4"
              >
                Logout
              </button>
            </div>
          </div>
        </div>
      )}
    </nav>
  )
}

export default Navbar
